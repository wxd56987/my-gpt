import { FC } from 'react'

interface Props {
}

const InsertBar: FC<Props> = ({}) => {
  return (
    <div
      className='w-2 h-4 bg-black inline-block relative top-1 insert-bar'
    >
    </div>
  )
}

export default InsertBar
